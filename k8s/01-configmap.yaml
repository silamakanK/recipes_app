apiVersion: v1
kind: ConfigMap
metadata:
  name: nutrismart-config
  namespace: nutrismart
data:
  NODE_ENV: "production"
  NEXT_TELEMETRY_DISABLED: "1"
  # URL interne du service PostgreSQL dans le cluster
  POSTGRES_HOST: "postgres-service"
  POSTGRES_PORT: "5432"
  POSTGRES_DB: "nutrismart"
